/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ExpenseCategoryService } from "../expenseCategory.service";
import { ExpenseCategoryCreateInput } from "./ExpenseCategoryCreateInput";
import { ExpenseCategoryWhereInput } from "./ExpenseCategoryWhereInput";
import { ExpenseCategoryWhereUniqueInput } from "./ExpenseCategoryWhereUniqueInput";
import { ExpenseCategoryFindManyArgs } from "./ExpenseCategoryFindManyArgs";
import { ExpenseCategoryUpdateInput } from "./ExpenseCategoryUpdateInput";
import { ExpenseCategory } from "./ExpenseCategory";
import { ExpenseFindManyArgs } from "../../expense/base/ExpenseFindManyArgs";
import { Expense } from "../../expense/base/Expense";
import { ExpenseWhereUniqueInput } from "../../expense/base/ExpenseWhereUniqueInput";
import { TagOrganizationExpenseCategoryFindManyArgs } from "../../tagOrganizationExpenseCategory/base/TagOrganizationExpenseCategoryFindManyArgs";
import { TagOrganizationExpenseCategory } from "../../tagOrganizationExpenseCategory/base/TagOrganizationExpenseCategory";
import { TagOrganizationExpenseCategoryWhereUniqueInput } from "../../tagOrganizationExpenseCategory/base/TagOrganizationExpenseCategoryWhereUniqueInput";

export class ExpenseCategoryControllerBase {
  constructor(protected readonly service: ExpenseCategoryService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ExpenseCategory })
  async create(
    @common.Body() data: ExpenseCategoryCreateInput
  ): Promise<ExpenseCategory> {
    return await this.service.create({
      data: {
        ...data,

        tenant: data.tenant
          ? {
              connect: data.tenant,
            }
          : undefined,

        organization: data.organization
          ? {
              connect: data.organization,
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,

        tenant: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [ExpenseCategory] })
  @ApiNestedQuery(ExpenseCategoryFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<ExpenseCategory[]> {
    const args = plainToClass(ExpenseCategoryFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,

        tenant: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ExpenseCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: ExpenseCategoryWhereUniqueInput
  ): Promise<ExpenseCategory | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,

        tenant: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ExpenseCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: ExpenseCategoryWhereUniqueInput,
    @common.Body() data: ExpenseCategoryUpdateInput
  ): Promise<ExpenseCategory | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          tenant: data.tenant
            ? {
                connect: data.tenant,
              }
            : undefined,

          organization: data.organization
            ? {
                connect: data.organization,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,

          tenant: {
            select: {
              id: true,
            },
          },

          organization: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ExpenseCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: ExpenseCategoryWhereUniqueInput
  ): Promise<ExpenseCategory | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,

          tenant: {
            select: {
              id: true,
            },
          },

          organization: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/expense")
  @ApiNestedQuery(ExpenseFindManyArgs)
  async findManyExpense(
    @common.Req() request: Request,
    @common.Param() params: ExpenseCategoryWhereUniqueInput
  ): Promise<Expense[]> {
    const query = plainToClass(ExpenseFindManyArgs, request.query);
    const results = await this.service.findExpense(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        amount: true,
        typeOfExpense: true,
        notes: true,
        currency: true,
        valueDate: true,
        purpose: true,
        taxType: true,
        taxLabel: true,
        rateValue: true,
        receipt: true,
        splitExpense: true,
        reference: true,
        status: true,

        organizationContact: {
          select: {
            id: true,
          },
        },

        expenseCategory: {
          select: {
            id: true,
          },
        },

        employee: {
          select: {
            id: true,
          },
        },

        tenant: {
          select: {
            id: true,
          },
        },

        organizationProject: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },

        organizationVendor: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/expense")
  async connectExpense(
    @common.Param() params: ExpenseCategoryWhereUniqueInput,
    @common.Body() body: ExpenseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      expense: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/expense")
  async updateExpense(
    @common.Param() params: ExpenseCategoryWhereUniqueInput,
    @common.Body() body: ExpenseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      expense: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/expense")
  async disconnectExpense(
    @common.Param() params: ExpenseCategoryWhereUniqueInput,
    @common.Body() body: ExpenseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      expense: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Get("/:id/tagOrganizationExpenseCategory")
  @ApiNestedQuery(TagOrganizationExpenseCategoryFindManyArgs)
  async findManyTagOrganizationExpenseCategory(
    @common.Req() request: Request,
    @common.Param() params: ExpenseCategoryWhereUniqueInput
  ): Promise<TagOrganizationExpenseCategory[]> {
    const query = plainToClass(
      TagOrganizationExpenseCategoryFindManyArgs,
      request.query
    );
    const results = await this.service.findTagOrganizationExpenseCategory(
      params.id,
      {
        ...query,
        select: {
          expenseCategory: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      }
    );
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/tagOrganizationExpenseCategory")
  async connectTagOrganizationExpenseCategory(
    @common.Param() params: ExpenseCategoryWhereUniqueInput,
    @common.Body() body: TagOrganizationExpenseCategoryWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tagOrganizationExpenseCategory: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/tagOrganizationExpenseCategory")
  async updateTagOrganizationExpenseCategory(
    @common.Param() params: ExpenseCategoryWhereUniqueInput,
    @common.Body() body: TagOrganizationExpenseCategoryWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tagOrganizationExpenseCategory: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/tagOrganizationExpenseCategory")
  async disconnectTagOrganizationExpenseCategory(
    @common.Param() params: ExpenseCategoryWhereUniqueInput,
    @common.Body() body: TagOrganizationExpenseCategoryWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tagOrganizationExpenseCategory: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
