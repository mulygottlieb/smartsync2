/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ImportRecordService } from "../importRecord.service";
import { ImportRecordCreateInput } from "./ImportRecordCreateInput";
import { ImportRecordWhereInput } from "./ImportRecordWhereInput";
import { ImportRecordWhereUniqueInput } from "./ImportRecordWhereUniqueInput";
import { ImportRecordFindManyArgs } from "./ImportRecordFindManyArgs";
import { ImportRecordUpdateInput } from "./ImportRecordUpdateInput";
import { ImportRecord } from "./ImportRecord";

export class ImportRecordControllerBase {
  constructor(protected readonly service: ImportRecordService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ImportRecord })
  async create(
    @common.Body() data: ImportRecordCreateInput
  ): Promise<ImportRecord> {
    return await this.service.create({
      data: {
        ...data,

        tenant: data.tenant
          ? {
              connect: data.tenant,
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        entityType: true,
        sourceId: true,
        destinationId: true,
        importDate: true,

        tenant: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [ImportRecord] })
  @ApiNestedQuery(ImportRecordFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<ImportRecord[]> {
    const args = plainToClass(ImportRecordFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        entityType: true,
        sourceId: true,
        destinationId: true,
        importDate: true,

        tenant: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ImportRecord })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: ImportRecordWhereUniqueInput
  ): Promise<ImportRecord | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        entityType: true,
        sourceId: true,
        destinationId: true,
        importDate: true,

        tenant: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ImportRecord })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: ImportRecordWhereUniqueInput,
    @common.Body() data: ImportRecordUpdateInput
  ): Promise<ImportRecord | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          tenant: data.tenant
            ? {
                connect: data.tenant,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          entityType: true,
          sourceId: true,
          destinationId: true,
          importDate: true,

          tenant: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ImportRecord })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: ImportRecordWhereUniqueInput
  ): Promise<ImportRecord | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          entityType: true,
          sourceId: true,
          destinationId: true,
          importDate: true,

          tenant: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
