/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import { PrismaService } from "../../prisma/prisma.service";

import {
  Prisma,
  Invite,
  InviteOrganizationContact,
  InviteOrganizationDepartment,
  InviteOrganizationProject,
  InviteOrganizationTeam,
  User,
  Organization,
  Tenant,
  Role,
} from "@prisma/client";

export class InviteServiceBase {
  constructor(protected readonly prisma: PrismaService) {}

  async count<T extends Prisma.InviteCountArgs>(
    args: Prisma.SelectSubset<T, Prisma.InviteCountArgs>
  ): Promise<number> {
    return this.prisma.invite.count(args);
  }

  async findMany<T extends Prisma.InviteFindManyArgs>(
    args: Prisma.SelectSubset<T, Prisma.InviteFindManyArgs>
  ): Promise<Invite[]> {
    return this.prisma.invite.findMany(args);
  }
  async findOne<T extends Prisma.InviteFindUniqueArgs>(
    args: Prisma.SelectSubset<T, Prisma.InviteFindUniqueArgs>
  ): Promise<Invite | null> {
    return this.prisma.invite.findUnique(args);
  }
  async create<T extends Prisma.InviteCreateArgs>(
    args: Prisma.SelectSubset<T, Prisma.InviteCreateArgs>
  ): Promise<Invite> {
    return this.prisma.invite.create<T>(args);
  }
  async update<T extends Prisma.InviteUpdateArgs>(
    args: Prisma.SelectSubset<T, Prisma.InviteUpdateArgs>
  ): Promise<Invite> {
    return this.prisma.invite.update<T>(args);
  }
  async delete<T extends Prisma.InviteDeleteArgs>(
    args: Prisma.SelectSubset<T, Prisma.InviteDeleteArgs>
  ): Promise<Invite> {
    return this.prisma.invite.delete(args);
  }

  async findInviteOrganizationContact(
    parentId: string,
    args: Prisma.InviteOrganizationContactFindManyArgs
  ): Promise<InviteOrganizationContact[]> {
    return this.prisma.invite
      .findUniqueOrThrow({
        where: { id: parentId },
      })
      .inviteOrganizationContact(args);
  }

  async findInviteOrganizationDepartment(
    parentId: string,
    args: Prisma.InviteOrganizationDepartmentFindManyArgs
  ): Promise<InviteOrganizationDepartment[]> {
    return this.prisma.invite
      .findUniqueOrThrow({
        where: { id: parentId },
      })
      .inviteOrganizationDepartment(args);
  }

  async findInviteOrganizationProject(
    parentId: string,
    args: Prisma.InviteOrganizationProjectFindManyArgs
  ): Promise<InviteOrganizationProject[]> {
    return this.prisma.invite
      .findUniqueOrThrow({
        where: { id: parentId },
      })
      .inviteOrganizationProject(args);
  }

  async findInviteOrganizationTeam(
    parentId: string,
    args: Prisma.InviteOrganizationTeamFindManyArgs
  ): Promise<InviteOrganizationTeam[]> {
    return this.prisma.invite
      .findUniqueOrThrow({
        where: { id: parentId },
      })
      .inviteOrganizationTeam(args);
  }

  async getUserInviteInvitedByIdTouser(parentId: string): Promise<User | null> {
    return this.prisma.invite
      .findUnique({
        where: { id: parentId },
      })
      .userInviteInvitedByIdTouser();
  }

  async getOrganization(parentId: string): Promise<Organization | null> {
    return this.prisma.invite
      .findUnique({
        where: { id: parentId },
      })
      .organization();
  }

  async getTenant(parentId: string): Promise<Tenant | null> {
    return this.prisma.invite
      .findUnique({
        where: { id: parentId },
      })
      .tenant();
  }

  async getRole(parentId: string): Promise<Role | null> {
    return this.prisma.invite
      .findUnique({
        where: { id: parentId },
      })
      .role();
  }

  async getUserInviteUserIdTouser(parentId: string): Promise<User | null> {
    return this.prisma.invite
      .findUnique({
        where: { id: parentId },
      })
      .userInviteUserIdTouser();
  }
}
