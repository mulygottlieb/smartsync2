/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ReportCategoryService } from "../reportCategory.service";
import { ReportCategoryCreateInput } from "./ReportCategoryCreateInput";
import { ReportCategoryWhereInput } from "./ReportCategoryWhereInput";
import { ReportCategoryWhereUniqueInput } from "./ReportCategoryWhereUniqueInput";
import { ReportCategoryFindManyArgs } from "./ReportCategoryFindManyArgs";
import { ReportCategoryUpdateInput } from "./ReportCategoryUpdateInput";
import { ReportCategory } from "./ReportCategory";
import { ReportFindManyArgs } from "../../report/base/ReportFindManyArgs";
import { Report } from "../../report/base/Report";
import { ReportWhereUniqueInput } from "../../report/base/ReportWhereUniqueInput";

export class ReportCategoryControllerBase {
  constructor(protected readonly service: ReportCategoryService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ReportCategory })
  async create(
    @common.Body() data: ReportCategoryCreateInput
  ): Promise<ReportCategory> {
    return await this.service.create({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        iconClass: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [ReportCategory] })
  @ApiNestedQuery(ReportCategoryFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<ReportCategory[]> {
    const args = plainToClass(ReportCategoryFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        iconClass: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ReportCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: ReportCategoryWhereUniqueInput
  ): Promise<ReportCategory | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        iconClass: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ReportCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: ReportCategoryWhereUniqueInput,
    @common.Body() data: ReportCategoryUpdateInput
  ): Promise<ReportCategory | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          iconClass: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ReportCategory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: ReportCategoryWhereUniqueInput
  ): Promise<ReportCategory | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          iconClass: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/report")
  @ApiNestedQuery(ReportFindManyArgs)
  async findManyReport(
    @common.Req() request: Request,
    @common.Param() params: ReportCategoryWhereUniqueInput
  ): Promise<Report[]> {
    const query = plainToClass(ReportFindManyArgs, request.query);
    const results = await this.service.findReport(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        slug: true,
        description: true,
        image: true,
        iconClass: true,
        showInMenu: true,

        reportCategory: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/report")
  async connectReport(
    @common.Param() params: ReportCategoryWhereUniqueInput,
    @common.Body() body: ReportWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      report: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/report")
  async updateReport(
    @common.Param() params: ReportCategoryWhereUniqueInput,
    @common.Body() body: ReportWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      report: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/report")
  async disconnectReport(
    @common.Param() params: ReportCategoryWhereUniqueInput,
    @common.Body() body: ReportWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      report: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
