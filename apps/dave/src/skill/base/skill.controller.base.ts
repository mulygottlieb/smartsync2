/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SkillService } from "../skill.service";
import { SkillCreateInput } from "./SkillCreateInput";
import { SkillWhereInput } from "./SkillWhereInput";
import { SkillWhereUniqueInput } from "./SkillWhereUniqueInput";
import { SkillFindManyArgs } from "./SkillFindManyArgs";
import { SkillUpdateInput } from "./SkillUpdateInput";
import { Skill } from "./Skill";
import { SkillEmployeeFindManyArgs } from "../../skillEmployee/base/SkillEmployeeFindManyArgs";
import { SkillEmployee } from "../../skillEmployee/base/SkillEmployee";
import { SkillEmployeeWhereUniqueInput } from "../../skillEmployee/base/SkillEmployeeWhereUniqueInput";
import { SkillOrganizationFindManyArgs } from "../../skillOrganization/base/SkillOrganizationFindManyArgs";
import { SkillOrganization } from "../../skillOrganization/base/SkillOrganization";
import { SkillOrganizationWhereUniqueInput } from "../../skillOrganization/base/SkillOrganizationWhereUniqueInput";

export class SkillControllerBase {
  constructor(protected readonly service: SkillService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Skill })
  async create(@common.Body() data: SkillCreateInput): Promise<Skill> {
    return await this.service.create({
      data: {
        ...data,

        tenant: data.tenant
          ? {
              connect: data.tenant,
            }
          : undefined,

        organization: data.organization
          ? {
              connect: data.organization,
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        description: true,
        color: true,

        tenant: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Skill] })
  @ApiNestedQuery(SkillFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<Skill[]> {
    const args = plainToClass(SkillFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        description: true,
        color: true,

        tenant: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Skill })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: SkillWhereUniqueInput
  ): Promise<Skill | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        description: true,
        color: true,

        tenant: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Skill })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: SkillWhereUniqueInput,
    @common.Body() data: SkillUpdateInput
  ): Promise<Skill | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          tenant: data.tenant
            ? {
                connect: data.tenant,
              }
            : undefined,

          organization: data.organization
            ? {
                connect: data.organization,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          description: true,
          color: true,

          tenant: {
            select: {
              id: true,
            },
          },

          organization: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Skill })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: SkillWhereUniqueInput
  ): Promise<Skill | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          description: true,
          color: true,

          tenant: {
            select: {
              id: true,
            },
          },

          organization: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/skillEmployee")
  @ApiNestedQuery(SkillEmployeeFindManyArgs)
  async findManySkillEmployee(
    @common.Req() request: Request,
    @common.Param() params: SkillWhereUniqueInput
  ): Promise<SkillEmployee[]> {
    const query = plainToClass(SkillEmployeeFindManyArgs, request.query);
    const results = await this.service.findSkillEmployee(params.id, {
      ...query,
      select: {
        employee: {
          select: {
            id: true,
          },
        },

        skill: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/skillEmployee")
  async connectSkillEmployee(
    @common.Param() params: SkillWhereUniqueInput,
    @common.Body() body: SkillEmployeeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      skillEmployee: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/skillEmployee")
  async updateSkillEmployee(
    @common.Param() params: SkillWhereUniqueInput,
    @common.Body() body: SkillEmployeeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      skillEmployee: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/skillEmployee")
  async disconnectSkillEmployee(
    @common.Param() params: SkillWhereUniqueInput,
    @common.Body() body: SkillEmployeeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      skillEmployee: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Get("/:id/skillOrganization")
  @ApiNestedQuery(SkillOrganizationFindManyArgs)
  async findManySkillOrganization(
    @common.Req() request: Request,
    @common.Param() params: SkillWhereUniqueInput
  ): Promise<SkillOrganization[]> {
    const query = plainToClass(SkillOrganizationFindManyArgs, request.query);
    const results = await this.service.findSkillOrganization(params.id, {
      ...query,
      select: {
        skill: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/skillOrganization")
  async connectSkillOrganization(
    @common.Param() params: SkillWhereUniqueInput,
    @common.Body() body: SkillOrganizationWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      skillOrganization: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/skillOrganization")
  async updateSkillOrganization(
    @common.Param() params: SkillWhereUniqueInput,
    @common.Body() body: SkillOrganizationWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      skillOrganization: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/skillOrganization")
  async disconnectSkillOrganization(
    @common.Param() params: SkillWhereUniqueInput,
    @common.Body() body: SkillOrganizationWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      skillOrganization: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
