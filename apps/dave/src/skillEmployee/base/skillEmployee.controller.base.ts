/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SkillEmployeeService } from "../skillEmployee.service";
import { SkillEmployeeCreateInput } from "./SkillEmployeeCreateInput";
import { SkillEmployeeWhereInput } from "./SkillEmployeeWhereInput";
import { SkillEmployeeWhereUniqueInput } from "./SkillEmployeeWhereUniqueInput";
import { SkillEmployeeFindManyArgs } from "./SkillEmployeeFindManyArgs";
import { SkillEmployeeUpdateInput } from "./SkillEmployeeUpdateInput";
import { SkillEmployee } from "./SkillEmployee";

export class SkillEmployeeControllerBase {
  constructor(protected readonly service: SkillEmployeeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SkillEmployee })
  async create(
    @common.Body() data: SkillEmployeeCreateInput
  ): Promise<SkillEmployee> {
    return await this.service.create({
      data: {
        ...data,

        employee: {
          connect: data.employee,
        },

        skill: {
          connect: data.skill,
        },
      },
      select: {
        employee: {
          select: {
            id: true,
          },
        },

        skill: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [SkillEmployee] })
  @ApiNestedQuery(SkillEmployeeFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<SkillEmployee[]> {
    const args = plainToClass(SkillEmployeeFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        employee: {
          select: {
            id: true,
          },
        },

        skill: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SkillEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: SkillEmployeeWhereUniqueInput
  ): Promise<SkillEmployee | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        employee: {
          select: {
            id: true,
          },
        },

        skill: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SkillEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: SkillEmployeeWhereUniqueInput,
    @common.Body() data: SkillEmployeeUpdateInput
  ): Promise<SkillEmployee | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          employee: {
            connect: data.employee,
          },

          skill: {
            connect: data.skill,
          },
        },
        select: {
          employee: {
            select: {
              id: true,
            },
          },

          skill: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SkillEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: SkillEmployeeWhereUniqueInput
  ): Promise<SkillEmployee | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          employee: {
            select: {
              id: true,
            },
          },

          skill: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
