/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SkillOrganizationService } from "../skillOrganization.service";
import { SkillOrganizationCreateInput } from "./SkillOrganizationCreateInput";
import { SkillOrganizationWhereInput } from "./SkillOrganizationWhereInput";
import { SkillOrganizationWhereUniqueInput } from "./SkillOrganizationWhereUniqueInput";
import { SkillOrganizationFindManyArgs } from "./SkillOrganizationFindManyArgs";
import { SkillOrganizationUpdateInput } from "./SkillOrganizationUpdateInput";
import { SkillOrganization } from "./SkillOrganization";

export class SkillOrganizationControllerBase {
  constructor(protected readonly service: SkillOrganizationService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SkillOrganization })
  async create(
    @common.Body() data: SkillOrganizationCreateInput
  ): Promise<SkillOrganization> {
    return await this.service.create({
      data: {
        ...data,

        skill: {
          connect: data.skill,
        },

        organization: {
          connect: data.organization,
        },
      },
      select: {
        skill: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [SkillOrganization] })
  @ApiNestedQuery(SkillOrganizationFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<SkillOrganization[]> {
    const args = plainToClass(SkillOrganizationFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        skill: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SkillOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: SkillOrganizationWhereUniqueInput
  ): Promise<SkillOrganization | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        skill: {
          select: {
            id: true,
          },
        },

        organization: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SkillOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: SkillOrganizationWhereUniqueInput,
    @common.Body() data: SkillOrganizationUpdateInput
  ): Promise<SkillOrganization | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          skill: {
            connect: data.skill,
          },

          organization: {
            connect: data.organization,
          },
        },
        select: {
          skill: {
            select: {
              id: true,
            },
          },

          organization: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SkillOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: SkillOrganizationWhereUniqueInput
  ): Promise<SkillOrganization | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          skill: {
            select: {
              id: true,
            },
          },

          organization: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
