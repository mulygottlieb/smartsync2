/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagCandidateService } from "../tagCandidate.service";
import { TagCandidateCreateInput } from "./TagCandidateCreateInput";
import { TagCandidateWhereInput } from "./TagCandidateWhereInput";
import { TagCandidateWhereUniqueInput } from "./TagCandidateWhereUniqueInput";
import { TagCandidateFindManyArgs } from "./TagCandidateFindManyArgs";
import { TagCandidateUpdateInput } from "./TagCandidateUpdateInput";
import { TagCandidate } from "./TagCandidate";

export class TagCandidateControllerBase {
  constructor(protected readonly service: TagCandidateService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagCandidate })
  async create(
    @common.Body() data: TagCandidateCreateInput
  ): Promise<TagCandidate> {
    return await this.service.create({
      data: {
        ...data,

        candidate: {
          connect: data.candidate,
        },

        tag: {
          connect: data.tag,
        },
      },
      select: {
        candidate: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagCandidate] })
  @ApiNestedQuery(TagCandidateFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagCandidate[]> {
    const args = plainToClass(TagCandidateFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        candidate: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagCandidate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagCandidateWhereUniqueInput
  ): Promise<TagCandidate | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        candidate: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagCandidate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagCandidateWhereUniqueInput,
    @common.Body() data: TagCandidateUpdateInput
  ): Promise<TagCandidate | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          candidate: {
            connect: data.candidate,
          },

          tag: {
            connect: data.tag,
          },
        },
        select: {
          candidate: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagCandidate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagCandidateWhereUniqueInput
  ): Promise<TagCandidate | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          candidate: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
