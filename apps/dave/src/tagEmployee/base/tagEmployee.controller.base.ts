/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagEmployeeService } from "../tagEmployee.service";
import { TagEmployeeCreateInput } from "./TagEmployeeCreateInput";
import { TagEmployeeWhereInput } from "./TagEmployeeWhereInput";
import { TagEmployeeWhereUniqueInput } from "./TagEmployeeWhereUniqueInput";
import { TagEmployeeFindManyArgs } from "./TagEmployeeFindManyArgs";
import { TagEmployeeUpdateInput } from "./TagEmployeeUpdateInput";
import { TagEmployee } from "./TagEmployee";

export class TagEmployeeControllerBase {
  constructor(protected readonly service: TagEmployeeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagEmployee })
  async create(
    @common.Body() data: TagEmployeeCreateInput
  ): Promise<TagEmployee> {
    return await this.service.create({
      data: {
        ...data,

        tag: {
          connect: data.tag,
        },

        employee: {
          connect: data.employee,
        },
      },
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagEmployee] })
  @ApiNestedQuery(TagEmployeeFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagEmployee[]> {
    const args = plainToClass(TagEmployeeFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagEmployeeWhereUniqueInput
  ): Promise<TagEmployee | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagEmployeeWhereUniqueInput,
    @common.Body() data: TagEmployeeUpdateInput
  ): Promise<TagEmployee | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          tag: {
            connect: data.tag,
          },

          employee: {
            connect: data.employee,
          },
        },
        select: {
          tag: {
            select: {
              id: true,
            },
          },

          employee: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagEmployeeWhereUniqueInput
  ): Promise<TagEmployee | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          tag: {
            select: {
              id: true,
            },
          },

          employee: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
