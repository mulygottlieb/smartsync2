/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagExpenseService } from "../tagExpense.service";
import { TagExpenseCreateInput } from "./TagExpenseCreateInput";
import { TagExpenseWhereInput } from "./TagExpenseWhereInput";
import { TagExpenseWhereUniqueInput } from "./TagExpenseWhereUniqueInput";
import { TagExpenseFindManyArgs } from "./TagExpenseFindManyArgs";
import { TagExpenseUpdateInput } from "./TagExpenseUpdateInput";
import { TagExpense } from "./TagExpense";

export class TagExpenseControllerBase {
  constructor(protected readonly service: TagExpenseService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagExpense })
  async create(
    @common.Body() data: TagExpenseCreateInput
  ): Promise<TagExpense> {
    return await this.service.create({
      data: {
        ...data,

        expense: {
          connect: data.expense,
        },

        tag: {
          connect: data.tag,
        },
      },
      select: {
        expense: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagExpense] })
  @ApiNestedQuery(TagExpenseFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagExpense[]> {
    const args = plainToClass(TagExpenseFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        expense: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagExpense })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagExpenseWhereUniqueInput
  ): Promise<TagExpense | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        expense: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagExpense })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagExpenseWhereUniqueInput,
    @common.Body() data: TagExpenseUpdateInput
  ): Promise<TagExpense | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          expense: {
            connect: data.expense,
          },

          tag: {
            connect: data.tag,
          },
        },
        select: {
          expense: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagExpense })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagExpenseWhereUniqueInput
  ): Promise<TagExpense | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          expense: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
