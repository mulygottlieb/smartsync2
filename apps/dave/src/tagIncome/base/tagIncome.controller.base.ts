/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagIncomeService } from "../tagIncome.service";
import { TagIncomeCreateInput } from "./TagIncomeCreateInput";
import { TagIncomeWhereInput } from "./TagIncomeWhereInput";
import { TagIncomeWhereUniqueInput } from "./TagIncomeWhereUniqueInput";
import { TagIncomeFindManyArgs } from "./TagIncomeFindManyArgs";
import { TagIncomeUpdateInput } from "./TagIncomeUpdateInput";
import { TagIncome } from "./TagIncome";

export class TagIncomeControllerBase {
  constructor(protected readonly service: TagIncomeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagIncome })
  async create(@common.Body() data: TagIncomeCreateInput): Promise<TagIncome> {
    return await this.service.create({
      data: {
        ...data,

        tag: {
          connect: data.tag,
        },

        income: {
          connect: data.income,
        },
      },
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        income: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagIncome] })
  @ApiNestedQuery(TagIncomeFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagIncome[]> {
    const args = plainToClass(TagIncomeFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        income: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagIncome })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagIncomeWhereUniqueInput
  ): Promise<TagIncome | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        income: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagIncome })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagIncomeWhereUniqueInput,
    @common.Body() data: TagIncomeUpdateInput
  ): Promise<TagIncome | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          tag: {
            connect: data.tag,
          },

          income: {
            connect: data.income,
          },
        },
        select: {
          tag: {
            select: {
              id: true,
            },
          },

          income: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagIncome })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagIncomeWhereUniqueInput
  ): Promise<TagIncome | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          tag: {
            select: {
              id: true,
            },
          },

          income: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
