/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagInvoiceService } from "../tagInvoice.service";
import { TagInvoiceCreateInput } from "./TagInvoiceCreateInput";
import { TagInvoiceWhereInput } from "./TagInvoiceWhereInput";
import { TagInvoiceWhereUniqueInput } from "./TagInvoiceWhereUniqueInput";
import { TagInvoiceFindManyArgs } from "./TagInvoiceFindManyArgs";
import { TagInvoiceUpdateInput } from "./TagInvoiceUpdateInput";
import { TagInvoice } from "./TagInvoice";

export class TagInvoiceControllerBase {
  constructor(protected readonly service: TagInvoiceService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagInvoice })
  async create(
    @common.Body() data: TagInvoiceCreateInput
  ): Promise<TagInvoice> {
    return await this.service.create({
      data: {
        ...data,

        tag: {
          connect: data.tag,
        },

        invoice: {
          connect: data.invoice,
        },
      },
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        invoice: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagInvoice] })
  @ApiNestedQuery(TagInvoiceFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagInvoice[]> {
    const args = plainToClass(TagInvoiceFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        invoice: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagInvoice })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagInvoiceWhereUniqueInput
  ): Promise<TagInvoice | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        tag: {
          select: {
            id: true,
          },
        },

        invoice: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagInvoice })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagInvoiceWhereUniqueInput,
    @common.Body() data: TagInvoiceUpdateInput
  ): Promise<TagInvoice | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          tag: {
            connect: data.tag,
          },

          invoice: {
            connect: data.invoice,
          },
        },
        select: {
          tag: {
            select: {
              id: true,
            },
          },

          invoice: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagInvoice })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagInvoiceWhereUniqueInput
  ): Promise<TagInvoice | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          tag: {
            select: {
              id: true,
            },
          },

          invoice: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
