/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagOrganizationService } from "../tagOrganization.service";
import { TagOrganizationCreateInput } from "./TagOrganizationCreateInput";
import { TagOrganizationWhereInput } from "./TagOrganizationWhereInput";
import { TagOrganizationWhereUniqueInput } from "./TagOrganizationWhereUniqueInput";
import { TagOrganizationFindManyArgs } from "./TagOrganizationFindManyArgs";
import { TagOrganizationUpdateInput } from "./TagOrganizationUpdateInput";
import { TagOrganization } from "./TagOrganization";

export class TagOrganizationControllerBase {
  constructor(protected readonly service: TagOrganizationService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagOrganization })
  async create(
    @common.Body() data: TagOrganizationCreateInput
  ): Promise<TagOrganization> {
    return await this.service.create({
      data: {
        ...data,

        organization: {
          connect: data.organization,
        },

        tag: {
          connect: data.tag,
        },
      },
      select: {
        organization: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagOrganization] })
  @ApiNestedQuery(TagOrganizationFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagOrganization[]> {
    const args = plainToClass(TagOrganizationFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        organization: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagOrganizationWhereUniqueInput
  ): Promise<TagOrganization | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        organization: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagOrganizationWhereUniqueInput,
    @common.Body() data: TagOrganizationUpdateInput
  ): Promise<TagOrganization | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          organization: {
            connect: data.organization,
          },

          tag: {
            connect: data.tag,
          },
        },
        select: {
          organization: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagOrganizationWhereUniqueInput
  ): Promise<TagOrganization | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          organization: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
