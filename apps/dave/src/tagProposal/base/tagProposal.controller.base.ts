/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagProposalService } from "../tagProposal.service";
import { TagProposalCreateInput } from "./TagProposalCreateInput";
import { TagProposalWhereInput } from "./TagProposalWhereInput";
import { TagProposalWhereUniqueInput } from "./TagProposalWhereUniqueInput";
import { TagProposalFindManyArgs } from "./TagProposalFindManyArgs";
import { TagProposalUpdateInput } from "./TagProposalUpdateInput";
import { TagProposal } from "./TagProposal";

export class TagProposalControllerBase {
  constructor(protected readonly service: TagProposalService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagProposal })
  async create(
    @common.Body() data: TagProposalCreateInput
  ): Promise<TagProposal> {
    return await this.service.create({
      data: {
        ...data,

        proposal: {
          connect: data.proposal,
        },

        tag: {
          connect: data.tag,
        },
      },
      select: {
        proposal: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagProposal] })
  @ApiNestedQuery(TagProposalFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagProposal[]> {
    const args = plainToClass(TagProposalFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        proposal: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagProposal })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagProposalWhereUniqueInput
  ): Promise<TagProposal | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        proposal: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagProposal })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagProposalWhereUniqueInput,
    @common.Body() data: TagProposalUpdateInput
  ): Promise<TagProposal | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          proposal: {
            connect: data.proposal,
          },

          tag: {
            connect: data.tag,
          },
        },
        select: {
          proposal: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagProposal })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagProposalWhereUniqueInput
  ): Promise<TagProposal | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          proposal: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
