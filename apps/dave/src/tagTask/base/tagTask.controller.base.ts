/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagTaskService } from "../tagTask.service";
import { TagTaskCreateInput } from "./TagTaskCreateInput";
import { TagTaskWhereInput } from "./TagTaskWhereInput";
import { TagTaskWhereUniqueInput } from "./TagTaskWhereUniqueInput";
import { TagTaskFindManyArgs } from "./TagTaskFindManyArgs";
import { TagTaskUpdateInput } from "./TagTaskUpdateInput";
import { TagTask } from "./TagTask";

export class TagTaskControllerBase {
  constructor(protected readonly service: TagTaskService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagTask })
  async create(@common.Body() data: TagTaskCreateInput): Promise<TagTask> {
    return await this.service.create({
      data: {
        ...data,

        task: {
          connect: data.task,
        },

        tag: {
          connect: data.tag,
        },
      },
      select: {
        task: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagTask] })
  @ApiNestedQuery(TagTaskFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagTask[]> {
    const args = plainToClass(TagTaskFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        task: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagTask })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagTaskWhereUniqueInput
  ): Promise<TagTask | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        task: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagTask })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagTaskWhereUniqueInput,
    @common.Body() data: TagTaskUpdateInput
  ): Promise<TagTask | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          task: {
            connect: data.task,
          },

          tag: {
            connect: data.tag,
          },
        },
        select: {
          task: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagTask })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagTaskWhereUniqueInput
  ): Promise<TagTask | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          task: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
