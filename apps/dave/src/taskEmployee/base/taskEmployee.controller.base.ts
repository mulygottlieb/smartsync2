/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TaskEmployeeService } from "../taskEmployee.service";
import { TaskEmployeeCreateInput } from "./TaskEmployeeCreateInput";
import { TaskEmployeeWhereInput } from "./TaskEmployeeWhereInput";
import { TaskEmployeeWhereUniqueInput } from "./TaskEmployeeWhereUniqueInput";
import { TaskEmployeeFindManyArgs } from "./TaskEmployeeFindManyArgs";
import { TaskEmployeeUpdateInput } from "./TaskEmployeeUpdateInput";
import { TaskEmployee } from "./TaskEmployee";

export class TaskEmployeeControllerBase {
  constructor(protected readonly service: TaskEmployeeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TaskEmployee })
  async create(
    @common.Body() data: TaskEmployeeCreateInput
  ): Promise<TaskEmployee> {
    return await this.service.create({
      data: {
        ...data,

        task: {
          connect: data.task,
        },

        employee: {
          connect: data.employee,
        },
      },
      select: {
        task: {
          select: {
            id: true,
          },
        },

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TaskEmployee] })
  @ApiNestedQuery(TaskEmployeeFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TaskEmployee[]> {
    const args = plainToClass(TaskEmployeeFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        task: {
          select: {
            id: true,
          },
        },

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TaskEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TaskEmployeeWhereUniqueInput
  ): Promise<TaskEmployee | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        task: {
          select: {
            id: true,
          },
        },

        employee: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TaskEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TaskEmployeeWhereUniqueInput,
    @common.Body() data: TaskEmployeeUpdateInput
  ): Promise<TaskEmployee | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          task: {
            connect: data.task,
          },

          employee: {
            connect: data.employee,
          },
        },
        select: {
          task: {
            select: {
              id: true,
            },
          },

          employee: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TaskEmployee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TaskEmployeeWhereUniqueInput
  ): Promise<TaskEmployee | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          task: {
            select: {
              id: true,
            },
          },

          employee: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
