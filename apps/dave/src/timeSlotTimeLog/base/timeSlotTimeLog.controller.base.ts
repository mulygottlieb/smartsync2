/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TimeSlotTimeLogService } from "../timeSlotTimeLog.service";
import { TimeSlotTimeLogCreateInput } from "./TimeSlotTimeLogCreateInput";
import { TimeSlotTimeLogWhereInput } from "./TimeSlotTimeLogWhereInput";
import { TimeSlotTimeLogWhereUniqueInput } from "./TimeSlotTimeLogWhereUniqueInput";
import { TimeSlotTimeLogFindManyArgs } from "./TimeSlotTimeLogFindManyArgs";
import { TimeSlotTimeLogUpdateInput } from "./TimeSlotTimeLogUpdateInput";
import { TimeSlotTimeLog } from "./TimeSlotTimeLog";

export class TimeSlotTimeLogControllerBase {
  constructor(protected readonly service: TimeSlotTimeLogService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TimeSlotTimeLog })
  async create(
    @common.Body() data: TimeSlotTimeLogCreateInput
  ): Promise<TimeSlotTimeLog> {
    return await this.service.create({
      data: {
        ...data,

        timeLog: {
          connect: data.timeLog,
        },

        timeSlot: {
          connect: data.timeSlot,
        },
      },
      select: {
        timeLog: {
          select: {
            id: true,
          },
        },

        timeSlot: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TimeSlotTimeLog] })
  @ApiNestedQuery(TimeSlotTimeLogFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TimeSlotTimeLog[]> {
    const args = plainToClass(TimeSlotTimeLogFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        timeLog: {
          select: {
            id: true,
          },
        },

        timeSlot: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TimeSlotTimeLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TimeSlotTimeLogWhereUniqueInput
  ): Promise<TimeSlotTimeLog | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        timeLog: {
          select: {
            id: true,
          },
        },

        timeSlot: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TimeSlotTimeLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TimeSlotTimeLogWhereUniqueInput,
    @common.Body() data: TimeSlotTimeLogUpdateInput
  ): Promise<TimeSlotTimeLog | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          timeLog: {
            connect: data.timeLog,
          },

          timeSlot: {
            connect: data.timeSlot,
          },
        },
        select: {
          timeLog: {
            select: {
              id: true,
            },
          },

          timeSlot: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TimeSlotTimeLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TimeSlotTimeLogWhereUniqueInput
  ): Promise<TimeSlotTimeLog | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          timeLog: {
            select: {
              id: true,
            },
          },

          timeSlot: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
