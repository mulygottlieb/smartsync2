/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { WarehouseMerchantService } from "../warehouseMerchant.service";
import { WarehouseMerchantCreateInput } from "./WarehouseMerchantCreateInput";
import { WarehouseMerchantWhereInput } from "./WarehouseMerchantWhereInput";
import { WarehouseMerchantWhereUniqueInput } from "./WarehouseMerchantWhereUniqueInput";
import { WarehouseMerchantFindManyArgs } from "./WarehouseMerchantFindManyArgs";
import { WarehouseMerchantUpdateInput } from "./WarehouseMerchantUpdateInput";
import { WarehouseMerchant } from "./WarehouseMerchant";

export class WarehouseMerchantControllerBase {
  constructor(protected readonly service: WarehouseMerchantService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: WarehouseMerchant })
  async create(
    @common.Body() data: WarehouseMerchantCreateInput
  ): Promise<WarehouseMerchant> {
    return await this.service.create({
      data: {
        ...data,

        merchant: {
          connect: data.merchant,
        },

        warehouse: {
          connect: data.warehouse,
        },
      },
      select: {
        merchant: {
          select: {
            id: true,
          },
        },

        warehouse: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [WarehouseMerchant] })
  @ApiNestedQuery(WarehouseMerchantFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<WarehouseMerchant[]> {
    const args = plainToClass(WarehouseMerchantFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        merchant: {
          select: {
            id: true,
          },
        },

        warehouse: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: WarehouseMerchant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: WarehouseMerchantWhereUniqueInput
  ): Promise<WarehouseMerchant | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        merchant: {
          select: {
            id: true,
          },
        },

        warehouse: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: WarehouseMerchant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: WarehouseMerchantWhereUniqueInput,
    @common.Body() data: WarehouseMerchantUpdateInput
  ): Promise<WarehouseMerchant | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          merchant: {
            connect: data.merchant,
          },

          warehouse: {
            connect: data.warehouse,
          },
        },
        select: {
          merchant: {
            select: {
              id: true,
            },
          },

          warehouse: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: WarehouseMerchant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: WarehouseMerchantWhereUniqueInput
  ): Promise<WarehouseMerchant | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          merchant: {
            select: {
              id: true,
            },
          },

          warehouse: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
